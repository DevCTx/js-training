╔════════════════════════════════════════════════════════════════════════════╗
║        FRONT-END vs BACK-END - GUIDE DE DÉCISION                          ║
╚════════════════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════════════
1️⃣  CRITÈRES DE SÉCURITÉ (Le plus important!)
═══════════════════════════════════════════════════════════════════════════════

🔴 TOUJOURS BACK-END si:
─────────────────────
✅ Accès à des données sensibles (mots de passe, tokens, clés API)
✅ Authentification & autorisation
✅ Opérations critiques (transactions financières, suppressions irréversibles)
✅ Validation de données importantes
✅ Accès à une base de données
✅ Intégration avec des services externes sécurisés

Exemple: Vérifier si un utilisateur a accès à un document
❌ MAUVAIS (Front-end):
  if (user.role === 'admin') { showDocument(); } // On peut modifier le JS!

✅ BON (Back-end):
  app.get('/document/:id', (req, res) => {
    if (!isAuthorized(req.user, id)) {
      return res.status(403).send('Accès refusé');
    }
    // Retourner le document
  });

═══════════════════════════════════════════════════════════════════════════════
2️⃣  CRITÈRES DE PERFORMANCE
═══════════════════════════════════════════════════════════════════════════════

🟢 FRONT-END si:
────────────────
✅ Action instantanée (pas de réseau nécessaire)
✅ Calculs légers
✅ Interaction immédiate avec l'UI
✅ Pas de données sensibles

Exemples:
- Afficher/masquer un menu
- Valider un format d'email avant envoi
- Calculer un sous-total dans un panier
- Vérifier si un champ est vide
- Animer une transition

🔴 BACK-END si:
───────────────
✅ Opération lourde (peut prendre du temps)
✅ Traitement de gros volumes de données
✅ Requête réseau inévitable
✅ Résultat dépend de données serveur

Exemples:
- Chercher parmi des millions de lignes en base de données
- Générer un PDF complexe
- Traiter une image (redimensionner, convertir)
- Envoyer un email
- Faire des appels API

═══════════════════════════════════════════════════════════════════════════════
3️⃣  CRITÈRES DE DONNÉES
═══════════════════════════════════════════════════════════════════════════════

🟢 FRONT-END si:
────────────────
✅ Les données sont publiques
✅ Les données viennent déjà du serveur (côté client)
✅ Pas de modification permanente

🔴 BACK-END si:
───────────────
✅ Persistance en base de données
✅ Les données affectent d'autres utilisateurs
✅ Les données doivent rester synchronisées
✅ Calcul basé sur des données confidentielles

Exemple: Validation d'un email unique
❌ MAUVAIS (Front-end):
  // Impossible: on ne connaît pas tous les emails de la BDD!
  
✅ BON (Back-end):
  app.post('/register', async (req, res) => {
    const exists = await User.findOne({ email: req.body.email });
    if (exists) {
      return res.status(400).send('Email déjà utilisé');
    }
  });

═══════════════════════════════════════════════════════════════════════════════
4️⃣  TABLEAU COMPARATIF
═══════════════════════════════════════════════════════════════════════════════

┌──────────────────────────┬───────────────┬──────────────────────────┐
│ Type d'action            │ Front-end     │ Back-end                 │
├──────────────────────────┼───────────────┼──────────────────────────┤
│ Afficher/masquer menu    │ ✅ OUI        │ ❌ Non                   │
│ Valider email (format)   │ ✅ OUI        │ ✅ OUI (confirmation)    │
│ Valider email (unique)   │ ❌ Non        │ ✅ OUI (obligatoire)     │
│ Animer un élément        │ ✅ OUI        │ ❌ Non                   │
│ Vérifier permissions     │ ❌ Non        │ ✅ OUI (obligatoire)     │
│ Calculer prix TTC        │ ✅ OUI        │ ✅ OUI (confirmation)    │
│ Sauvegarder en BDD       │ ❌ Non        │ ✅ OUI (obligatoire)     │
│ Envoyer email            │ ❌ Non        │ ✅ OUI (obligatoire)     │
│ Filtrer liste locale     │ ✅ OUI        │ ✅ OUI (pour perf)       │
│ Redimensionner image     │ ⚠️ Optionnel  │ ✅ OUI (qualité)         │
│ Trier des données        │ ✅ OUI        │ ✅ OUI (pour perf)       │
│ Authentifier utilisateur │ ❌ Non        │ ✅ OUI (obligatoire)     │
│ Rediriger utilisateur    │ ✅ OUI        │ ✅ OUI (selon cas)       │
│ Convertir devise         │ ⚠️ Front+BE   │ ✅ Back-end (source vrai)│
└──────────────────────────┴───────────────┴──────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
5️⃣  RÈGLE D'OR
═══════════════════════════════════════════════════════════════════════════════

⚠️  NE JAMAIS faire confiance au Front-end pour:
   - Sécurité (authentification, permissions)
   - Intégrité des données (validation, unicité)
   - Opérations sensibles (transactions, suppressions)

✅ TOUJOURS valider/vérifier au Back-end ce que le Front-end envoie

═══════════════════════════════════════════════════════════════════════════════
6️⃣  EXEMPLES CONCRETS
═══════════════════════════════════════════════════════════════════════════════

EXEMPLE 1: Panier d'achat
────────────────────────

Front-end:
  ✅ Afficher les produits
  ✅ Ajouter/retirer du panier localement
  ✅ Animer l'interface
  ✅ Valider le format de la carte bancaire (format seulement)
  ✅ Calculer le sous-total (optionnel, juste pour l'affichage)

Back-end:
  ✅ Valider que les produits existent
  ✅ Recalculer le prix réel (avec réductions, taxes)
  ✅ Vérifier les stocks
  ✅ Traiter le paiement
  ✅ Créer la commande
  ✅ Envoyer la confirmation email

EXEMPLE 2: Profil utilisateur
──────────────────────────────

Front-end:
  ✅ Afficher le formulaire
  ✅ Valider les formats (email, téléphone)
  ✅ Afficher les erreurs en temps réel
  ✅ Animer les transitions

Back-end:
  ✅ Authentifier l'utilisateur (vérifier qu'il existe vraiment)
  ✅ Vérifier les permissions (peut-il modifier CE profil?)
  ✅ Valider les données
  ✅ Sauvegarder en base de données
  ✅ Envoyer email de confirmation si changement critique

EXEMPLE 3: Recherche de produits
─────────────────────────────────

Front-end:
  ✅ Afficher les résultats
  ✅ Filtrer/trier localement (pour les données reçues)
  ✅ Pagination UX

Back-end:
  ✅ Chercher en base de données (avec millions d'enregistrements)
  ✅ Appliquer les filtres serveur (avec permission)
  ✅ Trier de façon efficace
  ✅ Retourner les données paginées

═══════════════════════════════════════════════════════════════════════════════
7️⃣  PATTERN: VALIDATION EN DEUX ÉTAPES
═══════════════════════════════════════════════════════════════════════════════

Le meilleur pattern combine Front-end et Back-end:

1️⃣  FRONT-END (UX rapide)
   └─ Validation rapide du format
   └─ Feedback immédiat
   └─ Empêche requête inutile

2️⃣  BACK-END (Sécurité)
   └─ Validation complète
   └─ Logique métier
   └─ Authentification/autorisation
   └─ Sauvegarde

Exemple avec un formulaire d'inscription:

// ========== FRONT-END (validation UX) ==========
function validateEmail(email) {
  const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return regex.test(email);
}

function onEmailChange(email) {
  if (!validateEmail(email)) {
    showError("Email invalide");
    return;
  }
  // Envoi au serveur seulement si format OK
  checkEmailAvailability(email);
}

// ========== BACK-END (validation sécurité) ==========
app.post('/register', async (req, res) => {
  const { email, password } = req.body;
  
  // Revalider au serveur!
  if (!isValidEmail(email)) {
    return res.status(400).send('Email invalide');
  }
  
  // Vérifier unicité (impossible côté client)
  const exists = await User.findOne({ email });
  if (exists) {
    return res.status(400).send('Email déjà utilisé');
  }
  
  // Hacher le mot de passe (jamais en clair)
  const hashedPassword = await bcrypt.hash(password, 10);
  
  // Sauvegarder
  const user = await User.create({ email, password: hashedPassword });
  res.status(201).json({ message: 'Utilisateur créé' });
});

═══════════════════════════════════════════════════════════════════════════════
8️⃣  QUESTIONS À SE POSER
═══════════════════════════════════════════════════════════════════════════════

Pour chaque action, posez-vous:

1. 🔐 "Est-ce une opération sensible?"
   → OUI = Back-end obligatoire

2. 🚫 "L'utilisateur peut-il contourner le code?"
   → OUI = Back-end obligatoire

3. 📊 "Ça affecte les données durables?"
   → OUI = Back-end obligatoire

4. 👥 "Ça affecte d'autres utilisateurs?"
   → OUI = Back-end obligatoire

5. ⚡ "C'est une action instantanée?"
   → OUI = Front-end peut le faire

6. 🎨 "C'est lié à l'UI/UX?"
   → OUI = Front-end peut le faire

═══════════════════════════════════════════════════════════════════════════════

RÉSUMÉ FINAL:

Front-end: Pour l'expérience utilisateur
Back-end: Pour la logique métier et la sécurité

Doute? → Mets-le en Back-end! C'est plus sûr.
